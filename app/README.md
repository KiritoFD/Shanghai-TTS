# App Workspace

主文档已经移动到仓库根目录：

- [README.md](../README.md)

这里不再维护独立长文档，避免和根目录说明重复、漂移。

如果你修改了下面这些内容，请同步更新根目录 `README.md`：

- `app.py` 的启动流程、端口、API
- `configs/runtime_paths.json` 的路径和模型文件名
- `tts_engine.py` 的加载 / 卸载行为
- `recall/` 的索引构造和服务协议
